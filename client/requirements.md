## Packages
framer-motion | Smooth page transitions and modal animations
date-fns | Formatting and parsing dates for bookings

## Notes
- Application uses demo session auth for authentication.
- API endpoints return full schema objects.
- Tailwind font families 'sans' and 'display' need to be configured in tailwind.config.ts if overridden.
